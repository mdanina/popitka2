# Quick Start Guide

## Установка за 3 команды

```bash
# 1. Клонируйте репозиторий (если еще не сделали)
# git clone <repository-url>
# cd popitka2

# 2. Запустите скрипт установки
./setup.sh

# 3. Проверьте систему (опционально)
python check_system.py
```

## Первое использование

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите транскрибацию
python main_pipeline.py your_audio.wav --lang ru --speakers 4
```

## Основные команды

### Русский язык

```bash
# Встреча с 3 спикерами
python main_pipeline.py meeting.wav --lang ru --speakers 3

# Лекция (1 спикер)
python main_pipeline.py lecture.mp3 --lang ru --speakers 1
```

### Английский язык

```bash
# Интервью с 2 спикерами
python main_pipeline.py interview.mp3 --lang en --speakers 2

# Подкаст с 4 спикерами
python main_pipeline.py podcast.wav --lang en --speakers 4
```

### Быстрая обработка

```bash
# Используйте модель medium для быстрой обработки
python main_pipeline.py audio.wav --model medium --lang ru
```

## Что вы получите

После обработки будут созданы файлы:

- `audio_transcript.txt` - Простой текст с таймстампами
- `audio_transcript.md` - Форматированный Markdown
- `audio_detailed.md` - Детальный отчет со статистикой

## Поддерживаемые форматы

- ✓ WAV (рекомендуется)
- ✓ MP3
- ✓ M4A
- ✓ FLAC
- ✓ OGG

## Параметры

- `--lang ru|en` - Язык аудио
- `--speakers 1-6` - Количество спикеров
- `--model tiny|base|small|medium|large|large-v3` - Размер модели
- `--no-clean` - Без предобработки аудио

## Рекомендации по выбору модели

| Ваша система | Рекомендуемая модель |
|--------------|----------------------|
| CPU | small или medium |
| GPU (4-6 GB VRAM) | medium или large |
| GPU (8+ GB VRAM) | large-v3 |

## Примерное время обработки

**10 минут аудио:**
- Model small: ~1-2 минуты (GPU)
- Model medium: ~3-4 минуты (GPU)
- Model large-v3: ~6-8 минут (GPU)

**На CPU время увеличивается в 3-5 раз.**

## Возникли проблемы?

```bash
# Проверьте систему
python check_system.py

# Проверьте FFmpeg
ffmpeg -version

# Проверьте CUDA
python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
```

## Дополнительная помощь

- Полная документация: `README.md`
- Подробное руководство: `USAGE.md`
- Примеры команд: `examples.sh`

---

**Готово! Начинайте транскрибировать! 🎙️**
