# Audio Transcriber with Speaker Diarization

Автоматическая транскрибация аудио с разделением по спикерам и таймстампами. Полностью локальное решение без отправки данных в облако.

## Возможности

- **Транскрибация**: Преобразование речи в текст с помощью OpenAI Whisper
- **Диаризация**: Автоматическое определение и разделение спикеров с помощью NVIDIA NeMo
- **Таймстампы**: Точные временные метки для каждого сегмента
- **Языки**: Поддержка русского и английского языков
- **Форматы**: Вывод в JSON, TXT и Markdown
- **Локальность**: Все данные остаются на вашем компьютере

## Системные требования

- Python 3.8+
- FFmpeg (для обработки аудио)
- CUDA-совместимая видеокарта (рекомендуется, но не обязательно)
- Минимум 8 GB RAM (16 GB рекомендуется для больших моделей)

## Установка

### 1. Установка FFmpeg

#### Ubuntu/Debian
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

#### macOS
```bash
brew install ffmpeg
```

#### Windows
Скачайте с [официального сайта](https://ffmpeg.org/download.html)

### 2. Установка Python зависимостей

```bash
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

**Важно:** Установка NeMo может занять некоторое время и требует около 2-3 GB дискового пространства.

### 3. Проверка установки

```bash
# Проверьте доступность CUDA (опционально)
python -c "import torch; print(f'CUDA available: {torch.cuda.is_available()}')"

# Проверьте FFmpeg
ffmpeg -version
```

## Быстрый старт

### Простой запуск (рекомендуется)

```bash
# Русский язык, до 6 спикеров
python main_pipeline.py your_audio.wav --lang ru --speakers 6

# Английский язык, до 3 спикеров
python main_pipeline.py meeting.mp3 --lang en --speakers 3
```

### Параметры

- `--lang` - Язык аудио (`ru` или `en`), по умолчанию `ru`
- `--speakers` - Максимальное количество спикеров (1-6), по умолчанию `6`
- `--no-clean` - Пропустить шаг очистки аудио
- `--model` - Размер модели Whisper (`tiny`, `base`, `small`, `medium`, `large`, `large-v3`), по умолчанию `large-v3`

## Примеры использования

### Базовое использование

```bash
# Транскрибация совещания на русском
python main_pipeline.py meeting.wav --lang ru --speakers 4

# Быстрая транскрибация с моделью medium
python main_pipeline.py lecture.mp3 --model medium --lang en
```

### Пошаговое использование

Если нужен больший контроль, можно запускать шаги отдельно:

#### Шаг 1: Очистка аудио (опционально)
```bash
python clean_audio.py meeting.wav
# Создаст: meeting_cleaned.wav
```

#### Шаг 2: Транскрибация
```bash
python transcribe_whisper.py meeting_cleaned.wav --lang ru --model large-v3
# Создаст: meeting_cleaned.json, meeting_cleaned.txt
```

#### Шаг 3: Диаризация
```bash
python diarize_nemo.py meeting_cleaned.wav meeting_cleaned.json --max-speakers 6
# Создаст: meeting_cleaned_tagged.json
```

#### Шаг 4: Конвертация в читаемые форматы
```bash
python convert_to_readable.py meeting_cleaned_tagged.json
# Создаст: meeting_cleaned_transcript.txt, meeting_cleaned_transcript.md, meeting_cleaned_detailed.md
```

## Выходные файлы

После завершения pipeline создаются следующие файлы:

1. **`audio_cleaned.wav`** - Очищенное аудио (если использовалась очистка)
2. **`audio.json`** - Сырые сегменты транскрипции от Whisper
3. **`audio.txt`** - Полный текст транскрипции
4. **`audio_tagged.json`** - Сегменты с привязкой спикеров
5. **`audio_transcript.txt`** - Транскрипция с таймстампами и спикерами
6. **`audio_transcript.md`** - Markdown версия транскрипции
7. **`audio_detailed.md`** - Детальная версия со статистикой

### Пример выходного файла (TXT)

```
[00:00:03] Speaker_1: Добрый день, коллеги! Начинаем наше совещание.
[00:00:08] Speaker_2: Здравствуйте! Спасибо, что собрались.
[00:00:12] Speaker_1: Давайте обсудим текущие задачи по проекту.
[00:00:18] Speaker_3: У меня есть несколько вопросов по архитектуре.
```

### Пример выходного файла (Markdown)

```markdown
# Transcription: meeting

**Duration:** 01:23:45
**Speakers:** 3

---

## Transcription

### Speaker_1

**[00:00:03]** Добрый день, коллеги! Начинаем наше совещание.

**[00:00:12]** Давайте обсудим текущие задачи по проекту.

### Speaker_2

**[00:00:08]** Здравствуйте! Спасибо, что собрались.
```

## Выбор модели Whisper

| Модель | Размер | Скорость | Качество | Рекомендуется для |
|--------|--------|----------|----------|-------------------|
| tiny | 39 MB | Очень быстро | Низкое | Быстрые тесты |
| base | 74 MB | Быстро | Базовое | Простые записи |
| small | 244 MB | Средне | Хорошее | Обычные встречи |
| medium | 769 MB | Медленно | Очень хорошее | Важные записи |
| large | 1550 MB | Очень медленно | Отличное | Критичные задачи |
| large-v3 | 1550 MB | Очень медленно | Лучшее | Максимальное качество |

## Оптимизация производительности

### Использование GPU

Для ускорения обработки убедитесь, что установлена поддержка CUDA:

```bash
# Проверьте версию PyTorch с CUDA
python -c "import torch; print(torch.version.cuda)"

# При необходимости переустановите PyTorch с CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### Рекомендации по размеру модели

- **Для CPU**: используйте `small` или `medium`
- **Для GPU (4-6 GB VRAM)**: используйте `medium` или `large`
- **Для GPU (8+ GB VRAM)**: используйте `large-v3`

## Устранение проблем

### Ошибка: "ffmpeg not found"

Установите FFmpeg (см. раздел Установка выше).

### Ошибка: "CUDA out of memory"

- Используйте меньшую модель Whisper
- Закройте другие приложения, использующие GPU
- Обработайте аудио на CPU (медленнее, но надежнее)

### Ошибка при загрузке модели NeMo

Некоторые модели NeMo могут требовать токен Hugging Face:

```bash
# Создайте токен на https://huggingface.co/settings/tokens
export HF_TOKEN="your_token_here"
```

### Плохое качество транскрибации

- Используйте большую модель Whisper
- Убедитесь, что выбран правильный язык
- Проверьте качество исходного аудио
- Попробуйте включить очистку аудио

### Неправильное разделение спикеров

- Укажите точное количество спикеров вместо максимального
- Убедитесь, что аудио содержит четкую речь разных людей
- Попробуйте использовать стерео-запись вместо моно

## Ограничения

- Диаризация работает лучше с качественными записями
- Перекрывающаяся речь может быть сложна для разделения
- Фоновый шум может влиять на точность
- Максимум 6 спикеров (ограничение текущей конфигурации)

## Расширенное использование

### Кастомные параметры Whisper

```bash
# С начальным промптом для улучшения точности
python transcribe_whisper.py meeting.wav --lang ru --prompt "Обсуждение: AI, ML, нейросети"

# С beam search для лучшего качества
python transcribe_whisper.py meeting.wav --beam_size 5

# С контекстом из предыдущего текста
python transcribe_whisper.py meeting.wav --condition
```

### Только конвертация формата

```bash
# Только TXT
python convert_to_readable.py meeting_tagged.json --format txt

# Только Markdown
python convert_to_readable.py meeting_tagged.json --format markdown

# Детальный отчет
python convert_to_readable.py meeting_tagged.json --format detailed
```

## Структура проекта

```
.
├── clean_audio.py           # Очистка аудио
├── transcribe_whisper.py    # Транскрибация с Whisper
├── diarize_nemo.py          # Диаризация с NeMo
├── convert_to_readable.py   # Конвертация в читаемые форматы
├── main_pipeline.py         # Основной пайплайн (всё в одном)
├── requirements.txt         # Python зависимости
└── README.md               # Эта документация
```

## Конфиденциальность

Все операции выполняются локально на вашем компьютере. Никакие данные не отправляются в облако или на внешние серверы. Модели загружаются один раз и кэшируются локально.

## Лицензия

MIT License - используйте свободно в личных и коммерческих проектах.

## Благодарности

- [OpenAI Whisper](https://github.com/openai/whisper) - отличная модель транскрибации
- [NVIDIA NeMo](https://github.com/NVIDIA/NeMo) - мощный toolkit для диаризации
- [FFmpeg](https://ffmpeg.org/) - универсальный инструмент обработки медиа

## Вопросы и помощь

При возникновении проблем:

1. Проверьте, что все зависимости установлены
2. Убедитесь, что FFmpeg доступен
3. Проверьте формат и качество входного аудио
4. Попробуйте использовать меньшую модель Whisper
5. Создайте issue с описанием проблемы и логами

---

**Приятного использования! 🎙️**
